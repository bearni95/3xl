engine-strict=true
# @testing-library/jest-dom/vitest imports `vitest` as an optional peer; hoist it
# so it resolves from jest-dom's location under pnpm's strict node_modules layout.
public-hoist-pattern[]=*vitest*
